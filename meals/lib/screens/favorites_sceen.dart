import 'package:flutter/material.dart';

class FaviroteScreen extends StatelessWidget {
  const FaviroteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Favirotes"),
    );
  }
}
